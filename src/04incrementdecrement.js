/**
 * INCREMENT (++) & DECREMENT (--) OPERATORS
 * 
 * Post-Increment (x++):
 * - Returns CURRENT value BEFORE incrementing
 * - let y = x++; → y gets old value, then x increments
 * 
 * Pre-Increment (++x):
 * - Returns value AFTER incrementing
 * - let y = ++x; → x increments first, then y gets new value
 * 
 * Post-Decrement (x--):
 * - Returns CURRENT value BEFORE decrementing
 * 
 * Pre-Decrement (--x):
 * - Returns value AFTER decrementing
 * 
 * Use-Cases in Testing:
 * - Loop counters: for (let i = 0; i < 10; i++)
 * - Array indexing with post-increment: arr[index++]
 * - Pre-increment in conditions: while (++count < limit)
 */
// ++ and --

// 1. post ()later increment


